<UserControl x:Class="Church_Presenter.Views.Planner.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border Background="#FAFBFF" BorderBrush="#E6E9F2" BorderThickness="1" Padding="10">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="300"/>
      </Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" >
        <TextBlock Text="Planner Saved" FontWeight="SemiBold"/>
        <TextBlock Text="Last saved: 10:24 AM" Foreground="{StaticResource MutedTextBrush}"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" >
        <TextBlock Text="Auto Save" Foreground="{StaticResource MutedTextBrush}"/>
        <TextBlock Text="Theme: Default" Foreground="{StaticResource MutedTextBrush}"/>
      </StackPanel>
    </Grid>
  </Border>
</UserControl>
