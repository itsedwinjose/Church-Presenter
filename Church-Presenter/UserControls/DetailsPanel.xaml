<UserControl x:Class="Church_Presenter.UserControls.DetailsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">
    <Border Style="{StaticResource CardBorder}" Margin="0,12,0,0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <TextBlock Text="Details" FontWeight="SemiBold" 
                           FontSize="{DynamicResource FontSizeTitle}"
                           Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,16"/>

                <!-- File Name -->
                <TextBlock Text="File Name" FontWeight="SemiBold" 
                           FontSize="{DynamicResource FontSizeSmall}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Style="{StaticResource ModernTextBox}" Margin="0,4,0,12"
                         Text="{Binding SelectedMedia.FileName}"/>

                <!-- Description -->
                <TextBlock Text="Description" FontWeight="SemiBold" 
                           FontSize="{DynamicResource FontSizeSmall}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Style="{StaticResource ModernTextBox}" Margin="0,4,0,12"
                         MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True"
                         Text="{Binding SelectedMedia.Description}"/>

                <!-- Category -->
                <TextBlock Text="Category" FontWeight="SemiBold" 
                           FontSize="{DynamicResource FontSizeSmall}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <ComboBox Style="{StaticResource ModernComboBox}" Margin="0,4,0,12"
                          Text="{Binding SelectedMedia.Category}">
                    <ComboBoxItem>General</ComboBoxItem>
                    <ComboBoxItem>Bible Images</ComboBoxItem>
                    <ComboBoxItem>Worship</ComboBoxItem>
                </ComboBox>

                <!-- Tags -->
                <TextBlock Text="Tags" FontWeight="SemiBold" 
                           FontSize="{DynamicResource FontSizeSmall}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Style="{StaticResource ModernTextBox}" Margin="0,4,0,12"
                         Text="{Binding SelectedMedia.Tags}"/>

                <!-- Divider -->
                <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,0,0,12"/>

                <!-- Button -->
                <Button Content="Save Changes" Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
